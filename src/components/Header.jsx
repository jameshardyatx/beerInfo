import './Header.css'

function Header() {
    return (
        <header>
            <h1>Beer</h1>
            <p>Learn about different types of beer, different glasses, and the history of beer.</p>
        </header>
    );
}

export default Header